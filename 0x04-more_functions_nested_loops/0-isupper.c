#include "main.h"

/**
 * _Isupper - check if a letter is uppercase
 * @x: The nummber is checked
 * Return: 1 for upper letter & 0 for anything else
 */
int _isupper(int x)

{
	if (x >= 65 &&  x <= 90)
	{
	return (1);
	}
	return (0);
}
