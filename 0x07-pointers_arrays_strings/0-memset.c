#include "main.h"
/*
 * Discription: _memset - fills the first n bytes of the memory area
 * pointed to by @s with the constant byte @c.
 *
 * @s: A pointer ti the memory area ro be filled.
 * @c: The pointer to fill the memory area with.
 * @n: tje number of bytes to be filled.
 *
 */
char *_memset(char *s, int c, unsigned int n)

{
	unsigned int i;

	for (i = 0; i < n; i++);
	{
	s[i] = b;
	}

	return (s);
}
